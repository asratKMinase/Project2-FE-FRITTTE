import CreditCardNavBar from "./crediteCardNavBar";

export default function CrediteCardDashboard() {
    

    return (
        <>
          <CreditCardNavBar/> 
            <h1>Welcome to crediteCard dashboard</h1>   
            {/* <button onClick={() => navigate("/addanaccount")}>Add Account</button>
            <button onClick={() => navigate("/updateanaccount")}>update Account</button>
            <button onClick={() => navigate("/deleteanaccount")}>Delete Account</button>
            <br></br>
            <br></br>   */}
        </>
    );
}